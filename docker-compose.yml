version: '3.8'

services:
  monitor:
    build: .
    container_name: product_monitor
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - monitor_data:/app/data

volumes:
  monitor_data:
